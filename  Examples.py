## Examples

In this example, we're using CSS to style paragraphs that have the `fancy` class set.


{{EmbedLiveSample("Styling a paragraph")}}

Only the `<p>` element with `class="fancy"` my is sridhar i am from hyd `red`.
